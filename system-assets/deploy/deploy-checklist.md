# 部署检查清单（v0.1）

## 部署前（必做）
- 构建目录已确认（避免在仓库根目录误构建）
- 环境变量：local/test/prod 已区分，且无缺失
- CORS：允许域名清单正确（prod 不要 *）
- 配额/超时：上传大小、函数超时、内存配置确认
- 日志：统一 traceId 已接入
- 冒烟测试：已在目标环境跑过并通过（保存输出）

## 部署后（必做）
- 线上 /health 通过
- 核心链路 smoke-tests 通过
- 错误码 & traceId 能定位
